{
  "name": "clube-pet-api",
  "version": "1.0.0",
  "description": "Sistema de gestão para o ClubePet - API Backend",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "npx prisma migrate deploy && npx prisma generate && node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "postinstall": "npx prisma generate"
  },
  "keywords": ["clubepet", "pet", "hotel", "api", "express", "prisma"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "prisma": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
